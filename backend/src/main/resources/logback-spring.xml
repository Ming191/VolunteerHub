<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    
    <springProperty scope="context" name="applicationName" source="spring.application.name" defaultValue="volunteerhub"/>
    <springProperty scope="context" name="environment" source="spring.profiles.active" defaultValue="local"/>
    
    <!-- Console appender with JSON format for Docker/Loki -->
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <version/>
                <message/>
                <loggerName/>
                <threadName/>
                <logLevel/>
                <stackTrace>
                    <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                        <maxDepthPerThrowable>10</maxDepthPerThrowable>
                        <maxLength>2048</maxLength>
                        <rootCauseFirst>true</rootCauseFirst>
                        <exclude>sun\.reflect\..*</exclude>
                        <exclude>java\.lang\.reflect\..*</exclude>
                        <exclude>net\.sf\.cglib\.proxy\..*</exclude>
                        <exclude>org\.springframework\.aop\..*</exclude>
                        <exclude>org\.springframework\.security\..*</exclude>
                        <exclude>org\.springframework\.web\..*</exclude>
                        <exclude>org\.apache\.catalina\..*</exclude>
                    </throwableConverter>
                </stackTrace>
                <pattern>
                    <pattern>
                        {
                            "trace_id": "%mdc{traceId}",
                            "span_id": "%mdc{spanId}",
                            "application": "${applicationName}",
                            "environment": "${environment}"
                        }
                    </pattern>
                </pattern>
                <mdc>
                    <excludeMdcKeyName>traceId</excludeMdcKeyName>
                    <excludeMdcKeyName>spanId</excludeMdcKeyName>
                </mdc>
            </providers>
        </encoder>
    </appender>

    <!-- Async wrapper for Console JSON to prevent blocking -->
    <appender name="ASYNC_CONSOLE_JSON" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="CONSOLE_JSON"/>
    </appender>

    <!-- File appender with JSON format (optional, for local file logging) -->
    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/volunteerhub.log</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSX</timestampPattern>
            <timeZone>UTC</timeZone>
            <customFields>{"application":"${applicationName}","environment":"${environment}"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger_name</logger>
                <thread>thread_name</thread>
                <level>level</level>
                <stackTrace>stack_trace</stackTrace>
            </fieldNames>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/volunteerhub-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Development profile: Human-readable console output -->
    <springProfile name="dev,local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        
        <!-- Adjust log levels for specific packages -->
        <logger name="com.cs2" level="DEBUG"/>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="org.hibernate" level="INFO"/>
    </springProfile>

    <!-- Production profile: JSON format for Loki -->
    <springProfile name="prod,docker">
        <root level="INFO">
            <appender-ref ref="ASYNC_CONSOLE_JSON"/>
        </root>
        
        <!-- Adjust log levels for specific packages -->
        <logger name="com.cs2" level="INFO"/>
        <logger name="org.springframework.web" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="io.micrometer" level="INFO"/>
    </springProfile>

    <!-- Default: Use JSON for Docker environments -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>
        
        <logger name="com.cs2" level="INFO"/>
    </springProfile>

    <!-- Reduce noise from specific libraries -->
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    <logger name="org.springframework.boot.autoconfigure" level="WARN"/>
    <logger name="org.springframework.boot.actuate" level="INFO"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="org.hibernate.SQL" level="INFO"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>
    <logger name="io.lettuce" level="WARN"/>
    <logger name="com.rabbitmq" level="INFO"/>
    <logger name="io.micrometer.core" level="INFO"/>
</configuration>
